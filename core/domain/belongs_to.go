package domain

var (
	ALREADY_BELONGS = "the link already belongs to the category"
	HAS_LINKS = "the category cannot be deleted because it has links"
	NOT_BELONGS = "the link does not belong to the category"
)
